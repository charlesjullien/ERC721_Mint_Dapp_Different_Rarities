const networkConfig = {
    31337: {
        name: "localhost"
    },
    80001: {
        name: "polygonMumbai"
    }
}

const developmentChains = ["hardhat", "localhost"]

module.exports = {
    networkConfig,
    developmentChains
}